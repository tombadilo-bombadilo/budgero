# `shared/` — FSD layer

Reusable code with **no business/domain knowledge**: UI kit, utilities, config,
base API client, generic hooks and types.

- **Alias:** `@shared/*`
- **May import from:** nothing (lowest layer).
- **Imported by:** every other layer.

See [Feature-Sliced Design](https://feature-sliced.design/).
